function diffMatrix = checkMatrix(inputMatrix, observedValue)
    % returns diffMatrix which is the difference between
    % inputMatrix and the scalar observedValue
    diffMatrix = inputMatrix - observedValue;
    
